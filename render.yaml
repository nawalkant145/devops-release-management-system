databases:
  - name: release-mgmt-db
    plan: free
    databaseName: release_mgmt
    user: admin

services:
  # Backend - Spring Boot API
  - type: web
    name: releasepilot-api
    plan: free
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    envVars:
      - key: SPRING_DATASOURCE_URL
        value: jdbc:postgresql://${RELEASE_MGMT_DB_HOST}:${RELEASE_MGMT_DB_PORT}/${RELEASE_MGMT_DB_DATABASE}
      - key: RELEASE_MGMT_DB_HOST
        fromDatabase:
          name: release-mgmt-db
          property: host
      - key: RELEASE_MGMT_DB_PORT
        fromDatabase:
          name: release-mgmt-db
          property: port
      - key: RELEASE_MGMT_DB_DATABASE
        fromDatabase:
          name: release-mgmt-db
          property: database
      - key: SPRING_DATASOURCE_USERNAME
        fromDatabase:
          name: release-mgmt-db
          property: user
      - key: SPRING_DATASOURCE_PASSWORD
        fromDatabase:
          name: release-mgmt-db
          property: password
      - key: SPRING_JPA_HIBERNATE_DDL_AUTO
        value: update
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 8080
      - key: SPRING_DATASOURCE_DRIVER_CLASS_NAME
        value: org.postgresql.Driver

  # Frontend - Static React App
  - type: web
    name: releasepilot-ui
    runtime: static
    buildCommand: cd frontend && npm ci && npm run build
    staticPublishPath: frontend/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://releasepilot-api.onrender.com/api
